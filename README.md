Web-name: Index.html
Style sheet name: Style.css
Image name: the-periodic-table.webp

Description:
The webpage displays the periodic table using a JavaScript container. As the webpage window gets smaller, the periodic table will ajust itself, until a breaking point where it changes to an image of a periodic table.
